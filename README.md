# IMDb-Sentiment-Classifier-using-TensorFlow
## Project Objective
To build a binary text classification model that predicts whether a movie review expresses a **positive** or **negative** sentiment using the **IMDb dataset** and deploy it for interactive testing using **Streamlit**.
